Open a python server on port 1234
    "python3 -m http.server 1234"

Setup a rule in the OUTPUT chain to redirect 8080 to 1234
    "sudo iptables -t nat -A OUTPUT -p tcp --dport 8080 -j DNAT --to-destination 127.0.0.1:1234"

Show routing information for a port 
    "sudo iptables-save"
    "sudo iptables -t nat -L -n -v "

Connect to the new port
    "curl localhost:8080"

Clean up
     "sudo iptables -t nat -D OUTPUT -p tcp --dport 8080 -j DNAT --to-destination 127.0.0.1:1234"   
